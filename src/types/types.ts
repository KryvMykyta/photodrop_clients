type StripeRequestBody = {
    successLink: string;
    failLink: string;
    albumID: string;
    phoneNumber: string;
}

type PhotoRecord = {
    successLink: string;
    failLink: string;
    albumID: string;
    phoneNumber: string;
}

